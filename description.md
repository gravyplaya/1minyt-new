## User request

Review the latest code in the project. Come up with any new features that may be valuable.

## Context

The project is a YouTube transcript/summarization tool (1minyt). Recent commits include a switch to the Innertube API for transcript fetching, a migration from SQLite to PostgreSQL, and a change to store token expiry in seconds instead of milliseconds. The assignee should review the current codebase and propose new features that would add value.
