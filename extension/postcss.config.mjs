/**
 * TAV-68: intentionally empty. Stops Vite from walking up and picking the
 * Next.js app's Tailwind PostCSS pipeline — the extension uses plain CSS.
 */
const config = { plugins: {} };

export default config;
